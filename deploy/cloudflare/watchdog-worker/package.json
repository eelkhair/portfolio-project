{
  "name": "watchdog-dns-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that probes elkhair.tech and flips landing DNS to the Pages backup on sustained failure.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.0.0"
  }
}
